# AccuKnox
AccuKnox AI/ML assignment


### Installation & Setup

This script requires external packages (`requests` for API calls and `matplotlib` for generating the analytical bar chart). 

To install the necessary dependencies, navigate to the project directory and execute:
```bash
pip install -r requirements.txt
```

Alternatively, you can install the packages individually:
```bash
pip install requests matplotlib
```